# Supabase project (Settings → API in the Supabase dashboard)
SUPABASE_URL=https://xxxxxxxx.supabase.co
SUPABASE_ANON_KEY=eyJ...

# Bootstrap password, only used until one is saved in the database. Once set
# from Configuración → Cambiar contraseña, the stored one wins and this is
# ignored.
APP_PASSWORD=change-me
# Any random string, used to sign the session cookie
APP_SESSION_SECRET=change-me-too
